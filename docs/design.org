#+TITLE: emu-gmm: Measure-Theoretic GMM
#+SUBTITLE: Estimation via \(\mathbb{E}_\mu\)
#+AUTHOR: Ethan Ligon
#+OPTIONS: toc:nil num:t todo:nil |:t minlevel:1
#+bibliography: ~/bibtex/main.bib
#+bibliography: refs.bib

* Introduction and Population Primitives

The framework is named for the operator at its centre: =emu= reads as \(\mathbb{E}_\mu\), the expectation under a measure \(\mu\). The central design claim is that one *polymorphic operator interface* --- implemented against empirical, analytical, or synthetic measures --- drives sample estimation, identification analysis, and simulation-based inference through a single computational pipeline. ("One interface", deliberately, rather than "one measure": the flagship empirical implementation is honestly an \(M\)-tuple of coordinate-specific measures sharing a support, and the covariance \([V_X]_{jk}\) is irreducibly /joint/ --- it needs the rows where both \(j\) and \(k\) are observed, so the per-coordinate-measure picture is clean only on the diagonal. The unification is polymorphism over the interface, not mathematical structure; Section 2 carries the details.)

The most important architectural commitment is that variance construction is *input-separable* from integration: \(\mathbb{E}_\mu[\psi]\) is a property of the measure; \(V_\mu(\theta)\) is a property of a separate =CovarianceStrategy= that can be swapped (iid vs cluster-robust vs replicate-weight) without changing what is being integrated, and the same =EmpiricalMeasure= drives any covariance strategy on the design-awareness ladder. This is a claim about independent /substitutability of inputs/, not about statistical independence of outputs: under the default continuously-updated weighting (and under two-step), \(\hat\theta\), the J-statistic, and the standard errors are all functions of \(V_\mu\), so changing the covariance strategy changes the estimates. The claim that /is/ unconditional: the moment expectation \(\mathbb{E}_\mu[\psi]\) at any given \(\theta\) is design-invariant --- only \(V\) changes (Section 2).

This document covers the interface algebra (Section 2), a worked Euler example (Section 3), differentiation (Section 4), estimation and inference (Section 5), and named-tensor boundary tooling (Section 6).

** Contract surface (implemented)

This document deliberately interleaves settled architecture, planned design, prospective sketches, and open theory; this subsection is the boundary between them. The table below is the implemented contract surface --- the menu an estimation call site composes today. Every row names a real export (importable from =emu_gmm=, except the =studies= caveat noted in its row), with one line of semantics --- what the thing /is/, not its status history --- and a module pointer relative to =src/emu_gmm/=. Names and pointers only, no counts, versions, or dates: when a menu item ships, its row is the only edit this subsection needs.

#+name: tab:contract-surface
#+caption: The implemented menu. Module paths are relative to =src/emu_gmm/=.
| Kind           | Name                    | Semantics                                                  | Module                           |
|----------------+-------------------------+------------------------------------------------------------+----------------------------------|
| Entry points   | =estimate=              | Minimise \(Q_\mu\); the single estimation entry point      | =estimator.py=                   |
|                | =build_estimator=       | Factory: bind once, re-run; runs share one anchor \(\tau\) | =estimator.py=                   |
|----------------+-------------------------+------------------------------------------------------------+----------------------------------|
| Measures       | =EmpiricalMeasure=      | Weighted data measure; \((N,M)\) mask, per-moment \(N_j\)  | =measures/empirical.py=          |
|                | =AnalyticalMeasure=     | Closed-form expectation under a population law             | =measures/analytical.py=         |
|                | =SyntheticMeasure=      | Frozen-draw (CRN) simulator; deterministic in \(\theta\)   | =measures/synthetic.py=          |
|----------------+-------------------------+------------------------------------------------------------+----------------------------------|
| Covariance     | =IIDCovariance=         | pweighted iid \(V_X\) with pairwise-overlap missingness    | =covariance/iid.py=              |
|                | =ClusteredCovariance=   | Cluster-total sandwich; optional per-pair dof factor       | =covariance/clustered.py=        |
|                | =StratifiedCovariance=  | Between-PSU Neyman variance, within-cell centering         | =covariance/stratified.py=       |
|                | =DesignAwareCovariance= | Mixed design/sampling assembly; ablatable cross block      | =covariance/stratified.py=       |
|                | =SumCovariance=         | Signed sum of strategies; =two_way_cluster= builder        | =covariance/sum.py=              |
|                | =AnalyticalCovariance=  | \(V(\theta)\) directly from the population law             | =covariance/analytical.py=       |
|                | =SyntheticCovariance=   | Variance of the simulated moment estimator                 | =covariance/synthetic.py=        |
|----------------+-------------------------+------------------------------------------------------------+----------------------------------|
| Weighting      | =Identity=              | \(\Lambda \equiv I\); first stage and diagnostics          | =weighting.py=                   |
|                | =Fixed=                 | Two-step: a precomputed \(V_0\) held fixed                 | =weighting.py=                   |
|                | =IteratedWeighting=     | Alternate fixed solves; refresh \(V\) between rounds       | =weighting.py=                   |
|                | =ContinuouslyUpdated=   | Default; \(V(\theta)\) at every evaluation (alias =CUE=)   | =weighting.py=                   |
|----------------+-------------------------+------------------------------------------------------------+----------------------------------|
| Regularization | =DiagonalTikhonov=      | Adaptive ridge on \(V\); signed-spectrum feasibility       | =regularization.py=              |
| Penalty        | =TikhonovPenalty=       | In-objective parameter penalty \(Q_\mu + p(\theta)\)       | =penalty.py=                     |
|----------------+-------------------------+------------------------------------------------------------+----------------------------------|
| Optimizers     | =optimistix_lm=         | JAX-native Levenberg--Marquardt (default)                  | =optimizer.py=                   |
|                | =scipy_lm=              | SciPy LM fallback; not jit/vmap-compatible                 | =optimizer.py=                   |
|                | =linear_solver=         | Certified one-step Gauss--Newton for affine residuals      | =optimizer.py=                   |
|                | =riemannian_lm=         | Manifold-aware LM; auto-routed on manifold parameters      | =manifolds/riemannian_lm.py=     |
|----------------+-------------------------+------------------------------------------------------------+----------------------------------|
| Geometry       | =Euclidean=             | Unconstrained leaf (the default geometry)                  | =manifolds/euclidean.py=         |
|                | =Positive=              | Positive scalar (open half-line)                           | =manifolds/positive.py=          |
|                | =PSDFixedRank=          | Rank-\(K\) PSD \(\Gamma = AA'\); gauge-aware quotient      | =manifolds/psd_fixed_rank.py=    |
|                | =Product=               | Cartesian product of manifolds                             | =manifolds/product.py=           |
|                | =ManifoldLeaf=          | One parameter array + its manifold; a =theta_init= leaf    | =manifolds/manifold_leaf.py=     |
|                | =ParameterSpace= / =on= | Field-to-manifold declaration; lowers to =ManifoldLeaf=    | =parameter_space.py=             |
|----------------+-------------------------+------------------------------------------------------------+----------------------------------|
| Inference      | =j_test=                | \(J\) at a supplied \(\theta_0\); no minimisation          | =inference/j_test.py=            |
|                | =k_statistic=           | Kleibergen K/S/J decomposition; gauge-aware                | =inference/k_statistic.py=       |
|                | =k_confidence_set=      | Identification-robust set by grid inversion                | =inference/confidence_set.py=    |
|                | =moment_wild_bootstrap= | Cluster-wild Rademacher/Mammen J bootstrap                 | =inference/wild_bootstrap.py=    |
|                | =cluster_bootstrap=     | Refit-based cluster bootstrap                              | =inference/cluster_bootstrap.py= |
|                | =adaptive_bootstrap=    | Precision-targeted stopping rule for batched bootstraps    | =inference/adaptive.py=          |
|----------------+-------------------------+------------------------------------------------------------+----------------------------------|
| Results        | =EstimationResult=      | Labelled \(\hat\theta\), SEs, J p-values, functional SEs   | =types.py=                       |
|                | =Diagnostics=           | Ridge \(\tau\), conditioning, health flags per fit         | =types.py=                       |
|                | =OptimizerInfo=         | Solver telemetry attached to every result                  | =types.py=                       |
|----------------+-------------------------+------------------------------------------------------------+----------------------------------|
| Numerics       | =ridge_inverse=         | One-shot adaptive ridge + Cholesky inverse                 | =numerics/ridge_inverse.py=      |
|----------------+-------------------------+------------------------------------------------------------+----------------------------------|
| Studies        | =emu_gmm.studies=       | Monte Carlo driver; subpackage-only (caveat below)         | =studies/=                       |

Each inference helper returns a typed result object exported alongside it (=JTestResult=, =KStatisticResult=, =KConfidenceSet=, =WildBootstrapResult=, =ClusterBootstrapResult=, and =AdaptiveBootstrapResult= with its target functionals =BootstrapMean= / =BootstrapSE= / =BootstrapQuantile= / =BootstrapPValue=). =EstimationResult= additionally carries =coef_table=, the gauge-invariant \(\Gamma\)-functional standard errors (=eigenvalue_se()= / =gamma_se()= / =functional_se(f)=), =record()= (whose stacked payload type =FitRecord= is exported), and =to_pickle= / =from_pickle=. The remaining top-level exports are public-named plumbing: the protocols user code can type against (=Measure=, =CovarianceStrategy=, =WeightingStrategy=, =RegularizationStrategy=, =PenaltyStrategy=, =Optimizer=, =StructuralModel=, =ManifoldParam=), the diagnostics builders (=build_diagnostics=, =build_optimizer_health=, =compute_cond_info=; =diagnostics.py=), =LabelContext=, and the typed input-validation error =Emu_GMM_DimensionError=.

/Studies caveat/: =emu_gmm.studies= is implemented but deliberately not re-exported at the package top level --- its surface gets a release of real use before being frozen into the namespace (#130; the subpackage docstring carries the decision). Import =monte_carlo_study=, =replicate=, =MCRecords=, =StudyResult=, and the summarizers from =emu_gmm.studies= directly.

Everything in this document that is not in the table is design discussion, and its status lives elsewhere:

- Roadmap :: Planned-but-unbuilt work (e.g. =ReplicateWeightCovariance=, SMM matching, alternative regularisers) is tracked in =docs/implementation-plan.org= Section 13 and GitHub issue #131; this document mentions such items in passing but does not own their status.
- Theory debt :: The gap between what the code computes and what has been proven about it is catalogued in the /Open Theoretical Questions/ subsection of Section 5 and carried formally in =docs/mcar-asymptotics.org=.

** Population Primitives
Let \(\Theta \subset \mathbb{R}^K\) denote a compact parameter space. The framework's underlying stochastic object is a structural residual function \(\psi: \mathcal{X} \times \Theta \to \mathbb{R}^M\), measurable in its first argument, evaluated at observations drawn from a population measure \(\mu_\text{pop}\) on \((\mathcal{X}, \mathcal{F})\). The model structure follows from these derived mappings:

- Parameter Vector :: \(\theta \in \Theta\), where \(\theta_0\) represents the unique true parameter vector.
- Population Moment Function :: \(m: \Theta \to \mathbb{R}^M\), the population expectation of \(\psi\):
  \[ m(\theta) = \mathbb{E}_{\mu_\text{pop}}[\psi(\cdot, \theta)] \]
  The fundamental identification restriction is \(m(\theta_0) = 0\). Sections 2-5 generalize from \(\mu_\text{pop}\) to whatever measure the user supplies.
- Variance-Covariance Mapping :: \(V(\theta)\), the \(M \times M\) population covariance matrix of \(\psi\) under \(\mu_\text{pop}\):
  \[ V(\theta) = \operatorname{Var}_{\mu_\text{pop}}[\psi(\cdot, \theta)] = \mathbb{E}_{\mu_\text{pop}}[\psi\,\psi'] - m(\theta)\, m(\theta)' \]
  Assumed positive definite at \(\theta_0\). /Sample-level convention/: \(V_X(\theta)\) estimates the variance of the sample moment /estimator/ \(\bar m_X(\theta)\) rather than the per-observation variance of \(\psi\); the \(1/N\) scaling is absorbed into \(V_X\) throughout (see /Architectural Core Highlights/ in Section 5).
- Optimal Weighting Matrix :: \(\Lambda(\theta) = V^{-1}(\theta)\), providing the metric for the parameter space.
- Population Jacobian Matrix :: \(G(\theta)\), the \(M \times K\) matrix of first derivatives:
  \[ G(\theta) = \frac{\partial m(\theta)}{\partial \theta'} \]

** Local Geometry and Information Bounds
The population criterion function \(Q(\theta)\) is defined as the quadratic form:
\[ Q(\theta) = m(\theta)' \Lambda(\theta) m(\theta) \]
By construction, \(Q(\theta) \geq 0\) for all \(\theta \in \Theta\), achieving its global minimum at \(Q(\theta_0) = 0\).

Taking the second derivative of \(Q(\theta)\) and evaluating it at the truth \(\theta_0\), the terms containing the first and second derivatives of \(\Lambda(\theta)\) as well as the second derivatives of \(m(\theta)\) vanish because \(m(\theta_0) = 0\). The population Hessian collapses precisely to:
\[ \nabla^2 Q(\theta_0) = 2 G(\theta_0)' \Lambda(\theta_0) G(\theta_0) = 2 \mathcal{I}(\theta_0) \]
where \(\mathcal{I}(\theta_0)\) is the asymptotic precision of efficient GMM for this moment system --- loosely, the GMM information matrix:
\[ \mathcal{I}(\theta_0) = G(\theta_0)' \Lambda(\theta_0) G(\theta_0) \]
This is the efficient-GMM analogue of the Cram\'er-Rao bound; it coincides with the latter only when the moments span the score and the data-generating process is correctly specified.

/Notation guard (factor of \(N\))/: the \(V(\theta)\), \(\Lambda(\theta)\), and \(\mathcal{I}(\theta_0)\) of this section are /per-observation/ (population) objects. Section 5 works in the sample-level convention where \(V_X\) is the variance of the moment /estimator/ (so \(V_X = O(1/N)\)); there, \(G' V_X^{-1} G\) is the /full-sample/ precision \(\Sigma_{\hat\theta}^{-1}\) --- the same symbols, \(N\) times the per-observation \(\mathcal{I}\) of this section. The convention is stated for \(V\) in Section 1's definition above; it applies to the information matrix identically, and a reader porting formulas from [cite/t:@hansen82] / [cite/t:@hayashi00] (who studentise by \(\sqrt N\) and keep \(O(1)\) variances) should translate both symbols at once.

* Mathematical Interfaces and Operational Decoupling

The architecture separates the model into orthogonal interfaces so that integration, variance construction, and structural specification can be substituted independently:

- =StructuralModel= --- the economic primitives (residuals and their gradients at a single observation).
- =Measure= --- an integration operator \(\mu\) over \((\mathcal{X}, \mathcal{F})\) exposing \(\mathbb{E}_\mu[\,\cdot\,]\).
- =CovarianceStrategy= --- a constructor that maps pointwise \(\psi\) values to the \(M \times M\) matrix \(V_\mu(\theta)\).
- =Context= --- a convenience bundler that ships a (measure, strategy) pair appropriate to a given modality.

=CovarianceStrategy= is parallel to (not nested under) =Measure= at the type level. Estimation is invoked as \(\text{Estimator}(\text{model},\, \text{measure},\, \text{strategy},\, \text{weighting})\) rather than \(\text{Estimator}(\text{model},\, \text{context})\); the =Context= type exists purely as sugar. This matters because the choice of variance construction is orthogonal to the choice of integration: a single =EmpiricalMeasure= can be paired with iid, cluster-robust, or replicate-weight variance strategies without changing the measure object, and the design-awareness ladder lives entirely on the strategy side.

** Primitives

*** The Model Primitive: StructuralModel
The =StructuralModel= abstraction encapsulates only the elementary, observation-level structural equations. It possesses no awareness of sample scale, sampling weights, missingness patterns, or the empirical design layout.

- *Elementary Function*: \(\psi(x, \theta)\), an \(M \times 1\) vector mapping an observation \(x \in \mathcal{X} \subseteq \mathbb{R}^D\) and parameter vector \(\theta \in \Theta\) to structural residuals (e.g., the raw Euler equation residual for an individual household).
- *Elementary Gradient*: \(\nabla_\theta \psi(x, \theta) = \frac{\partial \psi(x, \theta)}{\partial \theta'}\), an \(M \times K\) matrix of structural derivatives evaluated at a single point.

*** The Integration Primitive: Measure
The =Measure= interface (\(\mu\)) acts strictly as a functional integration operator over a measurable space \((\mathcal{X}, \mathcal{F})\). It is agnostic to whether \(\mu\) is composed of real-world survey responses, analytical density functions, or synthetic simulations, and exposes exactly two operations when evaluated against a =StructuralModel= at parameter vector \(\theta\):

- Expectation Mapping ::
   \[ \mathbb{E}_\mu[\psi] = \int_\mathcal{X} \psi(x, \theta) \, d\mu(x) \]
- Jacobian Integration ::
   \[ \mathbb{E}_\mu[\nabla_\theta \psi] = \int_\mathcal{X} \nabla_\theta \psi(x, \theta) \, d\mu(x) \]

The =Measure= is a /computational interface/, not a strict measure-theoretic object. Implementations may glue together coordinate-specific measures or other internal machinery as long as the interface contract is honored. Whether a particular implementation corresponds to a single \(\sigma\)-finite measure or, as in the empirical case below, an \(M\)-tuple of coordinate-specific measures sharing a support, is an implementation detail invisible to the rest of the framework. The architecture is unified by polymorphism over the interface, not by mathematical structure of the underlying measures.

Variance construction is /not/ a responsibility of the =Measure= --- it lives in =CovarianceStrategy=.

*** The Variance Primitive: CovarianceStrategy
The =CovarianceStrategy= interface (\(\Sigma\)) is responsible for constructing the \(M \times M\) covariance matrix \(V_\mu(\theta)\) of the sample moment vector. It consumes pointwise \(\psi(x_i, \theta)\) evaluations (for empirical and synthetic measures) or analytic integrands (for analytical measures), and produces \(V_\mu(\theta)\).

Separating variance construction from integration isolates procedural and design concerns --- clustering, stratification, replicate weighting, missingness handling, regularization --- entirely outside the core =Measure= definition. The same =EmpiricalMeasure= can be combined with multiple strategies; the strategy doesn't change what \(\mathbb{E}_\mu[\psi]\) is, only what \(V_\mu(\theta)\) is.

*** The Context Bundle
For a given modality (Empirical / Analytical / Synthetic), the natural pairing of a =Measure= with a default =CovarianceStrategy= is exposed as a =Context= type. A =Context= holds:
- the measure \(\mu\);
- a default covariance strategy \(\Sigma_\mu\);
- any modality-specific metadata (cluster IDs, replicate weights, frozen simulation draws, quadrature grids) consumed by the strategy.

The estimation API accepts either an unbundled =(measure, strategy)= pair or a =Context=. =Context= is sugar, not a third primitive --- everything it does is delegation to its components.

** Polymorphic Implementations

*** EmpiricalMeasure
This implementation maps to a physical dataset. Strictly speaking, \(\mu_X\) is not a single scalar measure but an \(M\)-tuple of coordinate-specific measures \(\{\mu_X^{(j)}\}_{j=1}^M\) sharing a common support; the matrix-valued shorthand below packages this \(M\)-tuple into a single expression.

The measure is represented as a discrete, empirical mass:
\[ d\mu_X = \mathbf{N}_X \sum_{i \in X} D_i w_i \delta_{x_i} \]
where \(\delta_{x_i}\) is a Dirac delta at observation \(x_i\); \(w_i = 1/\pi_i\) is the probability weight (inverse inclusion probability --- /pweight/ semantics, not aweight or fweight); and \(D_i = \operatorname{diag}(d_i)\) is an \(M \times M\) coordinate-masking matrix tracking missingness per moment coordinate (\(d_{ij} = 1\) if moment \(j\) is observable for unit \(i\), \(0\) otherwise).

The dimension-specific scales are managed by the \(M \times M\) diagonal matrix \(\mathbf{N}_X\):
\[ \mathbf{N}_X = \operatorname{diag}\left( \frac{1}{N_1}, \frac{1}{N_2}, \dots, \frac{1}{N_M} \right) \quad \text{where} \quad N_j = \sum_{i \in X} d_{ij}\, w_i \]

Reading coordinate-wise:
\[ \mathbb{E}_{\mu_X}[\psi]_j = \frac{1}{N_j} \sum_{i \in X} d_{ij} w_i\, \psi_j(x_i, \theta), \]
each moment is integrated against its own coordinate-specific measure, with missing observations dropped per coordinate rather than per row.

*The estimand caveat, up front (not an "open question").* Dropping per coordinate preserves the population estimand \(\mathbb{E}[\psi_j]\) only when observability is ignorable --- the framework's theory note carries the standard results through under *MCAR* (\(d_{ij}\) independent of \(\psi\); =mcar-asymptotics.org=). When observability is /economically selected/, the per-coordinate mean estimates \(\mathbb{E}[\psi_j \mid d_{ij} = 1]\), a different estimand. The leading case to worry about is exactly the one that motivates the mask: *limited participation*. Whether household \(i\) holds asset \(j\) is correlated with wealth and constraints, hence with the SDF, hence with \(\psi_j\) itself (MNAR); worse, the non-holder's first-order condition is an /inequality/, so the equality moment does not hold even conditionally. A consumer running the Euler example on holders-only returns must either defend ignorability for their selection mechanism, model participation, or read \(\hat\theta\) as a holders-conditional object. The planned in-framework remedy is the inverse-probability-weighting route under MAR sketched in =mcar-asymptotics.org= Section 7 (a propensity-weighted strategy; no change to =Measure= or =StructuralModel=); it is not implemented. /This caveat is the price of the convenience feature and applies whenever the mask is doing economic work rather than recording survey accidents./

*** Empirical Covariance Strategies
The design-awareness ladder lives here. The =EmpiricalMeasure= can be paired with any of the following:

- =IIDCovariance=: pweighted iid form with pairwise-overlap missingness handling. The element \((j,k)\) uses the rows where both \(\psi_j\) and \(\psi_k\) are observed:
  \[ [V_X(\theta)]_{jk} = \frac{1}{N_j N_k} \sum_{i \in X} d_{ij} d_{ik}\, w_i^2\, \psi_j(x_i, \theta)\, \psi_k(x_i, \theta) \]
  Single-stage SRS with optional pweights; the common case.

- =ClusteredCovariance(cluster_ids)=: cluster totals before outer product. With clusters indexed by \(c\):
  \[ [V_X(\theta)]_{jk} = \frac{1}{N_j N_k} \sum_c \Big(\sum_{i \in c} d_{ij} w_i\, \psi_j(x_i, \theta)\Big) \Big(\sum_{i \in c} d_{ik} w_i\, \psi_k(x_i, \theta)\Big) \]
  Required when within-cluster correlation matters (households within villages, students within schools) [cite:@cameron-miller15]. The =IIDCovariance= form is the special case where each cluster is a single observation. The optional =dof_correction=True= multiplies entry \((j,k)\) by the finite-cluster factor \(G_{jk}/(G_{jk}-1)\), where \(G_{jk}\) is the number of clusters holding an observed unit for /both/ \(j\) and \(k\); with complete data this collapses to the textbook scalar \(G/(G-1)\), and the per-pair form is the design-aware generalisation (the cluster analogue of =StratifiedCovariance='s own \(H/(H-1)\) Bessel factor and the available-pairs \(N_j N_k\) rule). It is off by default --- the bare sandwich underestimates the sampling variance with /few/ clusters, so the correction is recommended there.

- =StratifiedCovariance(psu_ids, cell_ids, stratum_ids)=: design-based (Neyman) between-PSU stratified variance for a stratified experiment with multiple PSUs per cell. Centering happens /within/ each cell \(c = (\text{stratum} \times \text{arm})\), so the stratum-additive form falls out automatically (cross-cell products are never formed):
  \[ V_X(\theta) = N_X\Big(\sum_c \Omega_c\Big) N_X, \qquad N_X = \operatorname{diag}(1/N_j), \]
  \[ [\Omega_c]_{jk} = \frac{H_{c,jk}}{H_{c,jk}-1} \sum_{g \in c} u_{g,jk}\,\big(t_{g,j} - \bar t^{(jk)}_{c,j}\big)\big(t_{g,k} - \bar t^{(jk)}_{c,k}\big), \]
  with PSU totals \(t_{g,j} = \sum_{i \in g} d_{ij} w_i \psi_j(x_i,\theta)\), support indicators \(s_{g,j} = \mathbf 1\{\sum_{i\in g} d_{ij} > 0\}\), pairwise overlap weight \(u_{g,jk} = s_{g,j} s_{g,k}\), and the *pair-specific* effective PSU count \(H_{c,jk} = \sum_{g\in c} u_{g,jk}\). The centering mean \(\bar t^{(jk)}_{c,j}\) averages \(t_{g,j}\) over exactly the \((j,k)\)-overlap PSUs --- /not/ over coordinate \(j\)'s own support. This per-pair count is the design-side analogue of the pairwise-overlap \(N_j N_k\) rule above: it is the single detail a hand-rolled port is most likely to drop, collapsing \(H_{c,jk}\) to a per-coordinate \(H_{c,j}\) and silently mis-centering every off-diagonal under unequal observability. A cell with \(H_{c,jk} < 2\) furnishes no between-PSU variance and contributes exactly zero to entry \((j,k)\). Implemented in =src/emu_gmm/covariance/stratified.py=; cross-checked against an independent numpy reference in =tests/covariance/test_stratified.py=. /Note/: the /centered/ per-pair-overlap form can be indefinite under unequal support with missingness --- the within-cell centering over pair-specific overlap sets is what breaks the sum-of-outer-products structure. (The /uncentered/ =IIDCovariance= / =ClusteredCovariance= forms are PSD by construction --- a sum of outer products under a diagonal \(1/(N_jN_k)\) congruence --- absent the per-pair =dof_correction= Hadamard factor, which can itself break PSD; the other non-PD sources on the ladder are signed sums (=SumCovariance=) and the =DesignAwareCovariance= glue.) The \(0.5(V+V^\top)\) symmetrise only removes round-off asymmetry, and the finite-sample non-PD risk is handled by the regularization layer (=DiagonalTikhonov=), not by any internal repair.

  /Finite-population correction./ The optional =fpc=True= multiplies every entry of a cell's block by the single /coordinate-independent/ scalar \((1 - H_{sD,c}/H_s)\), where \(H_{sD,c}\) is the populated-PSU count assigned to cell \(c\) and \(H_s\) is the stratum-total populated-PSU count across all arms. This is the resolved convention: the FPC is a property of the without-replacement /assignment/ of groups to cells, not of per-coordinate observability, which already enters through the per-pair dof \(H_{c,jk}/(H_{c,jk}-1)\) and the centering. Using the per-pair overlap \(H_{c,jk}\) in the numerator (the rejected alternative) would double-count observability and perversely /shrink/ the correction for sparser pairs; a per-pair /denominator/ \(H_{s,jk}\) would collapse to \(H_{c,jk}\) for a single-arm coordinate and zero its variance. The default =fpc=False= is conservative (never anti-conservative); the FPC belongs to the design-based estimand where there is no masking, while the masked GMM path runs =fpc=False=. (Resolved jointly with the consumer's design appendix; see =CLAUDE.md= commitment 10.)

- =DesignAwareCovariance=: the law-of-total-variance assembly \(V_X = V_{TT} + V_{SS} + V_{TS}\) for a moment vector mixing randomized (\(z_T\), design-driven) and sampled (\(z_S\)) instruments, partitioned by a =design_moment_mask=. It is a /composition/ of covariance strategies: the design block \(V_{TT}\) is delegated to a shared =StratifiedCovariance= (the design-exact known-\(\pi\) Neyman block --- the lower-variance payoff), the sampling block \(V_{SS}\) to a shared =ClusteredCovariance= (the /uncentered/ cluster-total form), and only the off-diagonal coupling \(V_{TS}\) is computed inline: a cluster-robust cross pass over the two coordinate sets, /estimated, not zeroed/ (its design contribution is higher-order under the conditionally-mean-zero null; eq:offdiag). All three blocks share one \(N_j\) Var(mean) scale. The cross block's clustering unit is the one carried by the supplied =sampling= strategy (/caller-controlled/): the appendix's within-stratum cross-arm term is captured only when =sampling= is built with stratum-level clustering, since \(z_T\) and \(z_S\) live in different arms. The =sampling= strategy's =dof_correction= flag is likewise /inherited by the cross pass/ (resolved convention, #119): with =dof_correction=True= every cross pair \((j,k)\) is inflated by the per-pair \(G_{jk}/(G_{jk}-1)\) counted over the sampling clusters supporting both coordinates --- symmetric with the \(V_{SS}\) treatment and consistent with the per-pair philosophy throughout this section; the design FPC, by contrast, stays inside \(V_{TT}\) only. When every coordinate is design-driven the assembly reduces /bit-for-bit/ to =StratifiedCovariance= via the shared engine. Note: because the design-exact \(V_{TT}\) is /glued/ to the sampling cross/\(V_{SS}\) blocks (rather than added as the literal \(V^{\mathrm{des}}+V^{\mathrm{smp}}\) sum), the result is /not/ guaranteed PSD --- it can be indefinite under missingness / few PSUs exactly as =StratifiedCovariance= can; no internal PD repair is done, and =DiagonalTikhonov= handles it. This is the /coupled/ composition a plain additive =SumCovariance= cannot express (the cross corner is not zero). /Cross-block materiality (#109)./ Whether the cross coupling actually moves the test is a first-class question, so the cross block is ablatable in-framework rather than hand-rolled downstream: =from_design_mask(..., couple=False)= returns the /block-diagonal/ \(V_{TT}\oplus V_{SS}\) (the \(V_{TS}=0\) counterfactual) on the same coords and shared \(N_j\) scale, and =.cross_block(psi, theta, measure)= returns the estimated \(V_{TS}\) alone (its (T,S)/(S,T) corners, zeros elsewhere) so a consumer can report \(\lVert V_{TS}\rVert/\lVert V_{TT}\oplus V_{SS}\rVert\). By construction =covariance(couple=True) = covariance(couple=False) + cross_block=; the default =couple=True= is bit-for-bit the original coupled assembly. /Known caveat (#145, on the #130 MC evidence)./ The composition is asymmetric about centering: \(V_{TT}\) centers within (stratum \(\times\) arm) cells while the delegated \(V_{SS}\) and the inline \(V_{TS}\) are /uncentered/, so fixed stratum-mean heterogeneity contaminates the coupled assembly --- in the validation fixture the =couple=True= variance under-covers the randomized contrast (b1 coverage 0.66, ridge binding 39%) while plain =StratifiedCovariance= stays calibrated. Under stratum heterogeneity prefer =StratifiedCovariance=, or =couple=False=, until the v2.1 centering pass.

- =SumCovariance=: the /additive/ half of the composable-covariance algebra --- a signed sum \(V_X = \sum_k s_k V_k\) of child strategies (\(s_k \in \{+1, -1\}\)). The canonical consumer, and the reason the primitive exists rather than being guessed at, is /multiway (two-way) clustering/ (Cameron--Gelbach--Miller): \(V = V_g + V_h - V_{g\cap h}\), the inclusion--exclusion combination of one-way clusterings on two cross-cutting dimensions (build it with =SumCovariance.two_way_cluster=, which composes three =ClusteredCovariance= passes on \(g\), \(h\), and the intersection \(g\cap h\)). Every summand must be an empirical strategy on the /same/ measure so they share one \(N_j\) and the \(1/(N_jN_k)\) scale (commitment 9); the =cached_intermediates= 4-tuple is threaded unchanged to every child (one =vmap(psi)= pass). Under subtraction the result is /not/ guaranteed PSD (the known multiway-clustering non-PD risk); no internal repair --- =DiagonalTikhonov= handles it. =SumCovariance= and =DesignAwareCovariance= are the two halves design note #81 anticipated: the former for /independent/ components, the latter (a /joint/ object) for /coupled/ ones where the cross block is not zero.

- /Planned, not implemented/ (roadmap --- Section 1's contract surface): =ReplicateWeightCovariance= (BRR, jackknife, bootstrap variants for federal-survey datasets that distribute design information as replicate weight columns) [cite:@wolter07].

- /Out of scope/: single-PSU strata collapse rules; calibration / raking weights whose values depend on the realized sample.

Both =IIDCovariance= and =ClusteredCovariance= use /pairwise overlap/ (a.k.a. /available-pairs/) to avoid listwise deletion: each \([V_X]_{jk}\) uses every row that supports its pair, not just rows complete on every moment. The cost is that \(V_X\) is not guaranteed positive definite in finite samples --- see the regularization policy in Section 5.

*** Scaling convention --- per-coordinate \(N_j\), and why downstream code must not reinvent it
The two definitions just given are a /matched pair/, and the match is load-bearing. The moment is the per-coordinate mean \(\mathbb{E}_{\mu_X}[\psi]_j = \tfrac{1}{N_j}\sum_i d_{ij} w_i \psi_j\) (normalised by /its own/ \(N_j\)), and the variance carries the matching per-coordinate normalisation \([V_X]_{jk} \propto \tfrac{1}{N_j N_k}\). The criterion of Section 5 is then simply
\[ Q_X(\theta) = \mathbb{E}_{\mu_X}[\psi]'\, V_X(\theta)^{-1}\, \mathbb{E}_{\mu_X}[\psi] \xrightarrow{d} \chi^2_{M-K}, \]
with /no explicit factor of \(N\) anywhere/: the sample-size scaling lives entirely inside \(V_X\) (which is \(O(1/N)\)), and the heterogeneity in per-moment observation counts is carried by the \(\tfrac{1}{N_j N_k}\) structure (=mcar-asymptotics.org= Theorems 3 and 6, via the Hadamard factor \(p_{jk}/(p_j p_k) = \lim N^2/(N_j N_k)\)).

This is /algebraically identical/ to the conventional textbook form a reader may expect --- scale moment \(j\) by \(\sqrt{N_j}\) (turning it into an \(O_p(1)\) studentised sum \(g_j = \sqrt{N_j}\,\bar m_j\)) and weight by the \(O(1)\) asymptotic variance \(W_{jk} = \sqrt{N_j N_k}\,[V_X]_{jk}\):
\[ g'\,W^{-1}\,g \;=\; \mathbb{E}_{\mu_X}[\psi]'\, V_X^{-1}\, \mathbb{E}_{\mu_X}[\psi], \]
because the quadratic form is invariant under the joint rescaling \(m \mapsto Dm,\ V \mapsto DVD\) with \(D = \operatorname{diag}(\sqrt{N_j})\). =emu-gmm= simply pushes /all/ the \(N_j\) bookkeeping into \(V_X\) and keeps the moments as raw means.

*The trap.* The two forms coincide /only/ when the per-moment \(N_j\) are scaled consistently on both sides. Scaling the moments by \(\sqrt{N_j}\) while weighting by the per-sample \(V_X\) (i.e. \(g'V_X^{-1}g\), forgetting to rescale \(V\) to \(W\)) equals \(N \cdot Q_X\) /only when every \(N_j\) is equal/ --- the complete-data / no-missingness case where \(\sqrt{N_j N_k} = N\). Under genuine missingness (unequal \(N_j\)) it is a different, mis-weighted quadratic form with no \(\chi^2\) limit, and it mis-weights \(\hat\theta\) as well. Crucially, /a balanced Monte Carlo with =mask= all-ones cannot detect this/, because it forces \(N_j \equiv N\); the regression guard =tests/test_estimator_unequal_nj.py= exercises deliberately unequal \(N_j\) precisely to pin the distinction.

*The downstream contract.* The practical consequence is a rule, not just a caution: *downstream code must obtain the criterion value, \(J\)-statistic, standard errors, and \(p\)-values from the package* (=EstimationResult=, =k_statistic=, the inference helpers) and must /never/ recompute the criterion inline. Per-moment observability is expressed in exactly one place --- the \((N, M)\) =mask= on =EmpiricalMeasure= --- and the user's only modelling input is the per-observation residual \(\psi(x_i, \theta) \to \mathbb{R}^M\). A hand-rolled criterion is how the \(\sqrt{N_j}\)-vs-\(V_X\) mismatch above gets reintroduced. If an interface is missing a knob or appears to give a wrong answer, the correct response is to *file an =emu-gmm= issue, not to reimplement the statistic locally*: the package's job is to be the single spare, correct implementation of these quantities, and a local reimplementation forfeits that guarantee for everyone downstream of it.

*** EmpiricalContext
Bundles an =EmpiricalMeasure= with a default =CovarianceStrategy= (=ClusteredCovariance= when cluster IDs are present in the metadata, =IIDCovariance= otherwise) plus the dataset itself.

A structural property worth highlighting: \(\mathbb{E}_{\mu_X}[\psi]\) is design-invariant across every covariance strategy on the ladder. Only \(V_X(\theta)\) changes. The residual path through the estimator therefore needs no design-specific code; specialization lives entirely inside =CovarianceStrategy=.

*** AnalyticalMeasure
Contains no empirical data matrix. Represents the true continuous or discrete population distribution governed by the economic environment (e.g., joint log-normal income and preference shocks). Integration is performed via numerical quadrature (Gauss-Hermite, Gauss-Legendre) or exact closed-form solutions where available.

*** AnalyticalCovariance
Computes \(V(\theta)\) directly from the population density \(f\):
\[ V(\theta) = \int_\mathcal{X} \psi(x, \theta)\psi(x, \theta)'\, f(x) \, dx - \mathbb{E}_\mu[\psi]\, \mathbb{E}_\mu[\psi]' \]
typically via the same quadrature grid used by the measure.

*** AnalyticalContext
Bundles the =AnalyticalMeasure= and =AnalyticalCovariance= for an exhaustive specification of the true data-generating process. This pairing is the workhorse of the identification-verification diagnostic discussed in Section 5: at the known true value \(\theta_0\) the theoretical criterion satisfies \(Q(\theta_0) = 0\) exactly, and the numerical optimizer should converge to \(\theta_0\) to within quadrature and floating-point tolerance.

*** SyntheticMeasure
A stochastic parametric simulator whose law depends on \(\theta\). Three design choices keep it well-behaved:

1. *Common Random Numbers (CRN)*: the measure holds a frozen batch of \(N_{sim}\) latent draws \(\{z_s\}\) (a fixed =PRNGKey= in JAX) and recomputes observations \(x_s(\theta) = f(z_s; \theta)\) from the /same/ \(z_s\) at each \(\theta\). This converts a stochastic objective into a deterministic surface in \(\theta\), restoring the smoothness gradient-based optimisers require. Fresh draws at every evaluation produce \(\theta\)-varying Monte Carlo noise that defeats both quasi-Newton and trust-region methods.
2. *Reparameterization for derivatives*: when \(f(z; \theta)\) is smooth in \(\theta\) and \(z\) comes from a \(\theta\)-free base distribution, the Leibniz exchange holds inside the simulator:
   \[ \nabla_\theta \mathbb{E}_z[\psi(f(z;\theta), \theta)] = \mathbb{E}_z[\nabla_\theta \psi(f(z;\theta), \theta)] \]
   and JAX propagates the gradient end-to-end. For distributions that cannot be reparameterised (discrete latents, \(\theta\)-dependent support), the framework supports score-function (REINFORCE) gradients as an opt-in fallback, with the variance caveats that implies.
3. *Simulation cardinality*: \(N_{sim}\) is exposed as part of the =SyntheticMeasure='s state; the inference engine consumes it (alongside the empirical \(N\)) to assemble the canonical \((1 + 1/S)\) SMM inflation factor where \(S = N_{sim}/N\).

*** SyntheticCovariance
The variance of the simulated moment estimator \(\bar m_{sim}(\theta) = \mathbb{E}_{\mu_\text{sim}(\theta)}[\psi]\), scaled consistently with the empirical-side convention (variance of the moment estimator, not of an individual draw). For iid simulation draws:
\[ V_\text{sim}(\theta) = \frac{1}{N_{sim}}\Big( \mathbb{E}_z[\psi\, \psi'] - \mathbb{E}_z[\psi]\, \mathbb{E}_z[\psi]' \Big) \]
When combined additively with \(V_X\) in a matching SMM estimator, the result yields the standard \((1+1/S)\) inflation factor mechanically, without a separate correction.

*** SyntheticContext
Bundles =SyntheticMeasure= and =SyntheticCovariance= with the frozen \(z_s\) batch and any auxiliary metadata (e.g., the choice of reparameterization function). When combined with an =EmpiricalContext= in a matching estimator, this yields a Simulated Method of Moments estimator [cite:@mcfadden89;@pakes-pollard89] that is differentiable, JIT-compilable, and design-aware on the empirical side.

** Architectural Utility of Data-Free Measures
Decoupling the measure from physical data files introduces major diagnostic and theoretical capabilities at the design level:

1. *Deterministic Identification Verifications*: A =StructuralModel= can be integrated over an =AnalyticalContext= parameterized at a known true value \(\theta_0\). The analytical moment condition satisfies \(\int \psi(x, \theta_0) \, d\mu_\text{analytical} = 0\) exactly; numerically, the optimizer should converge to \(\theta_0\) within quadrature and floating-point precision. Failures beyond that tolerance isolate a global identification failure or an ill-behaved objective surface, independent of sample noise --- see the diagnostics taxonomy in Section 5.
2. *Ex-Ante Efficiency Mapping*: Evaluating the system curvature using an =AnalyticalContext= yields the true population information matrix \(\mathcal{I}(\theta_0) = G(\theta_0)' \Lambda(\theta_0) G(\theta_0)\). This enables the programmatic evaluation of alternative moment configurations (\(\psi\)) to maximize the information envelope before field data is ever introduced.

* Worked Example --- Consumption Euler Equation

To demonstrate the abstraction concretely, consider a Hansen--Singleton-style multi-asset consumption Euler equation under power utility. The structural parameters are \(\theta = (\beta, \gamma) \in \Theta \subset \mathbb{R}^2\) (so \(K = 2\)) where \(\beta\) is the discount factor and \(\gamma\) is the coefficient of relative risk aversion. With \(J\) risky assets, the observation vector is \(x_t = (c_t,\, c_{t+1},\, r_{1,t+1},\, \dots,\, r_{J,t+1}) \in \mathbb{R}^{2+J}\): current consumption, realised next-period consumption, and the realised gross returns on the \(J\) assets. The bundled example in =src/emu_gmm/examples/euler.py= uses \(J = 3\), giving \(M = 3\) and \(M - K = 1\) overidentifying restriction --- the smallest \(J\) for which the system admits a proper J-test.

** The StructuralModel
There is one Euler-equation residual per asset:
\[ \psi_j(x_t, \theta) = \beta \left( \frac{c_{t+1}}{c_t} \right)^{-\gamma} (1 + r_{j,t+1}) - 1, \qquad j = 1, \dots, J \]
so \(\psi(x_t, \theta) \in \mathbb{R}^M\) with \(M = J\). Each component shares the same stochastic discount factor \(\beta\,(c_{t+1}/c_t)^{-\gamma}\); the assets differ only through their gross returns.

The analytical \(M \times K\) Jacobian, evaluated at a single observation, has rows
\[ \nabla_\theta \psi_j(x_t, \theta) = \begin{bmatrix} \left( \tfrac{c_{t+1}}{c_t} \right)^{-\gamma} (1 + r_{j,t+1}) & -\beta \left( \tfrac{c_{t+1}}{c_t} \right)^{-\gamma} (1 + r_{j,t+1}) \ln\!\left( \tfrac{c_{t+1}}{c_t} \right) \end{bmatrix}, \]
so for \(J = 3\) the per-observation Jacobian is a \(3 \times 2\) matrix. The =StructuralModel= holds the residual function and its (autodiffed) Jacobian and nothing else --- no data, no quadrature grids, no simulation seeds.

A note on the simulator side: the =SyntheticMeasure= sampler used by the bundled example is intentionally \(\theta\)-independent --- the DGP is fixed at the truth \((\beta_0, \gamma_0)\) and \(\theta\) is what we are trying to recover from those draws. This is the standard identification round-trip shortcut: the sampler's =theta= argument is accepted for protocol conformance and discarded (=del theta=). The reparameterisation gradient story of Section 4 still applies whenever a real \(\theta\)-dependent simulator is supplied; this is the same accommodation called out in =api-sketch.org= Section 5. /Honesty note/: this means the differentiable-\(\theta\)-dependent-simulator path --- the very capability the CRN + reparameterisation machinery of Section 4 exists for --- is /asserted but not exercised/ by any shipped example or acceptance test; it becomes load-bearing only with SMM matching (deferred, #127). The accurate reading of the "three measures, one model" triptych in the current release is: /empirical estimates, analytical diagnoses, synthetic round-trips/.

A second note, on missingness: the worked example as shipped runs with =mask=ones= (complete data). The real-data version of this exact setup --- per-asset returns observed only for holders --- is the limited-participation case flagged in Section 2's estimand caveat: holding status is not ignorable for the Euler residual, and the non-holder FOC is an inequality. Running this example on real holders-only data without a participation correction estimates a holders-conditional object, not \(\mathbb{E}[\psi_j] = 0\).

** Three Measure / Strategy Pairs, One Model
The same =StructuralModel= instance is evaluated against three orthogonal /(measure, strategy)/ pairs without altering its code path. Per Section 2, =Context= is sugar rather than a third primitive; only the unbundled call signature =estimate(model, measure, *, covariance, weighting, optimizer, theta_init)= is implemented, so the diagram below shows the (Measure, CovarianceStrategy) pair that an estimation call site actually supplies:

#+BEGIN_SRC text
                            +------------------------------+
                            |   StructuralModel (Euler)    |
                            |   defines psi(x, theta)      |
                            +--------------+---------------+
                                           |
             +-----------------------------+-----------------------------+
             v                             v                             v
 +-----------------------+     +-----------------------+     +-----------------------+
 |   EmpiricalMeasure    |     |  AnalyticalMeasure    |     |  SyntheticMeasure     |
 +-----------------------+     +-----------------------+     +-----------------------+
 | CEX panel:            |     | Joint log-normal      |     | Log-normal preference |
 | (c_t, c_{t+1}, r_j)   |     | consumption /return   |     | shock simulator;      |
 | weights, cluster IDs. |     | population density.   |     | frozen z_s batch.     |
 +-----------------------+     +-----------------------+     +-----------------------+
 | ClusteredCovariance:  |     | AnalyticalCovariance: |     | SyntheticCovariance:  |
 | design-robust V_X     |     | Gauss-Legendre        |     | iid variance over     |
 | from cluster totals.  |     | quadrature on exact   |     | simulation draws,     |
 |                       |     | population means.     |     | combined w/ V_X for   |
 |                       |     |                       |     | (1+1/S) SMM inflation.|
 +-----------------------+     +-----------------------+     +-----------------------+
            \                            |                            /
             \                           |                           /
              \                          v                          /
               +--------------> estimate(model, measure, <----------+
                                covariance=..., weighting=...,
                                optimizer=..., theta_init=...)
#+END_SRC

Three estimation paths, one =StructuralModel=:

- /Empirical/: \(\hat\theta = \arg\min_\theta Q_X(\theta)\) on CEX household data with cluster-robust variance; =estimate(model, EmpiricalMeasure(...), covariance=ClusteredCovariance(...), ...)=.
- /Analytical/: the theoretical \(\arg\min_\theta Q_\text{analytical}(\theta)\) is \(\theta_0\) with \(Q = 0\) exactly; numerically, deviations beyond quadrature tolerance diagnose identification or conditioning failures, not sample noise.
- /Synthetic/: simulate \(\{(c_t, c_{t+1}, r_{1,t+1}, \dots, r_{J,t+1})\}_{t=1}^{N_\text{sim}}\) from a generative model parameterised at \(\theta\), hand to the same estimator. SMM matching (combining the synthetic measure with an empirical measure on a shared moment vector) is planned, not implemented (#127).

The same =psi=, the same gradient, the same objective: only the (measure, covariance) pair changes.

* Differentiation Architecture and Gradient Propagation

A critical practical challenge in executing structural estimation is the efficient and accurate propagation of derivatives through the model components. Because the objective function is minimized using gradient-based numerical optimization, and because the computation of the sample information matrix requires the evaluation of the population Jacobian, differentiation must be seamlessly wired across the code blocks.

Rather than treating differentiation as a global monolithic process, this architecture isolates automatic differentiation (AD) at the individual observation level while leveraging the linearity of the =Measure= abstraction to handle aggregation.

** The Computational Boundary for Automatic Differentiation
Running reverse-mode or forward-mode automatic differentiation over a massive empirical dataset can lead to catastrophic memory explosion (graph bloat) if the tracking engine logs every individual observation loop, sampling weight multiplication, and cluster-level accumulation.

To circumvent this, the AD graph is strictly isolated at the element-wise boundary:

- Observation-Level Graphing :: The system computes the structural error \(\psi(x, \theta)\) and its local gradient \(\nabla_\theta \psi(x, \theta)\) at a single point \(x\).
- Vectorized Batches (Vmap) :: For efficient hardware execution (e.g., SIMD lanes or GPU execution), the observation-level execution graph is batched across points using single-instruction, multiple-data vectorization primitives.
- Graph Detachment :: The tracking graph for automatic differentiation terminates immediately after delivering the pointwise matrices. The subsequent linear operations --- applying masking indicators (\(D_i\)) and structural weights (\(w_i\)) --- are executed via standard native tensor math, bypassing the AD overhead entirely.

This isolation strategy presumes the measure \(\mu\) itself does not depend on \(\theta\). For =EmpiricalMeasure= and =AnalyticalMeasure= with fixed quadrature grids this holds exactly. For =SyntheticMeasure=, where the law of the draws depends on \(\theta\), the isolation is maintained at the level of the latent variates: the frozen \(z_s\) batch carries no AD graph, while \(f(z_s; \theta)\) and \(\psi(f(z_s;\theta), \theta)\) are traced as usual.

** The Linearity of Gradient Integration
By the Leibniz Integral Rule, under standard regularity conditions allowing the exchange of differentiation and integration, the Jacobian mapping of the measure is equivalent to the integrated expectation of the local gradients.

For any valid =Measure= \(\mu\) whose support does not depend on \(\theta\), the functional interface maps the derivative through the integration space linearly:
\[ G_\mu(\theta) = \frac{\partial}{\partial \theta'} \int \psi(x, \theta) \, d\mu(x) = \int \left[ \nabla_\theta \psi(x, \theta) \right] \, d\mu(x) \]

- In an =AnalyticalMeasure=, this maps to the direct integration of the analytical derivative mapping over the continuous density.
- In an =EmpiricalMeasure=, this maps to the coordinate-scaled matrix product:
  \[ G_X(\theta) = \mathbf{N}_X \sum_{i \in X} D_i w_i \left[ \nabla_\theta \psi(x_i, \theta) \right] \]
- In a =SyntheticMeasure= with reparameterised draws, Leibniz applies in the latent space: \(G_\mu(\theta) = \mathbb{E}_z[\nabla_\theta \psi(f(z;\theta), \theta)]\), including the contribution that flows through \(f\).

** Differentiating through the Continuously Updating Weighting Matrix
Because this system targets the optimal GMM framework where the weighting matrix can be treated as a continuous function of the parameter vector itself, evaluating the full gradient of the criterion function requires differentiating through a matrix dependency.

Let \(Q_\mu(\theta) = \mathbb{E}_\mu[\psi]' V_\mu^{-1}(\theta) \mathbb{E}_\mu[\psi]\). The exact analytical gradient with respect to a scalar parameter \(\theta_k\) is:
\[ \frac{\partial Q_\mu(\theta)}{\partial \theta_k} = 2 \left[ \frac{\partial \mathbb{E}_\mu[\psi]}{\partial \theta_k} \right]' V_\mu^{-1}(\theta) \mathbb{E}_\mu[\psi] + \mathbb{E}_\mu[\psi]' \left[ \frac{\partial V_\mu^{-1}(\theta)}{\partial \theta_k} \right] \mathbb{E}_\mu[\psi] \]

Using the matrix derivative identity for an inverted matrix, the derivative of the optimal weighting metric maps directly back to the derivative of the variance:
\[ \frac{\partial V_\mu^{-1}(\theta)}{\partial \theta_k} = -V_\mu^{-1}(\theta) \left[ \frac{\partial V_\mu(\theta)}{\partial \theta_k} \right] V_\mu^{-1}(\theta). \]
Substituting yields the complete gradient vector:
\[ \nabla_\theta Q_\mu(\theta) = 2 G_\mu(\theta)' V_\mu^{-1}(\theta) \mathbb{E}_\mu[\psi] - \mathbb{E}_\mu[\psi]' V_\mu^{-1}(\theta) \left[ \nabla_\theta V_\mu(\theta) \right] V_\mu^{-1}(\theta) \mathbb{E}_\mu[\psi] \]

*** Asymptotic Equivalence of CU and Iterated Weighting
At the true parameter values (\(\theta_0\) in the population, or asymptotically at \(\hat{\theta}\)), \(\mathbb{E}_\mu[\psi(\cdot, \theta_0)] \xrightarrow{p} 0\). The second term in the gradient is a quadratic form in the moment vector and vanishes at rate \(O_p(N^{-1})\), while the primary term vanishes at \(O_p(N^{-1/2})\):
\[ \mathbb{E}_\mu[\psi]' V_\mu^{-1}(\theta) \left[ \nabla_\theta V_\mu(\theta) \right] V_\mu^{-1}(\theta) \mathbb{E}_\mu[\psi] \xrightarrow{p} 0 \]

This is the theoretical justification for why the /Iterated/ weighting strategy (which fixes \(V\) during each inner solve and refreshes it between iterations, so its inner gradient never sees \(\nabla_\theta V\)) converges to the same fixed point as the /Continuously Updated/ strategy (which sees \(\nabla_\theta V\) at every evaluation). It is *not* a license to drop \(\nabla_\theta V\) from the CU gradient implementation: away from the optimum the dropped term is \(O_p(1)\), and silently omitting it produces an estimator that is neither CU nor Iterated.

The clean architectural resolution --- see Section 5 --- is to make weighting an explicit strategy choice and to write the residual function so that JAX's AD picks up exactly the right gradient for each strategy.

For the information matrix used by the inference engine, \(\mathcal{I}_\mu(\hat\theta) = G_\mu(\hat\theta)' \Lambda_\mu(\hat\theta) G_\mu(\hat\theta)\) --- computed directly from \(G\) and \(\Lambda\), not from a numerical Hessian of \(Q\). The asymptotic argument above implies the two coincide at \(\hat\theta\); in finite samples they need not, and the direct \(G'\Lambda G\) form is both cheaper and more stable.

* Estimation, Decomposition, and Specification Testing

** The Decomposition-Driven Criterion, Q_\mu(\theta)
To ensure numerical stability and prevent precision loss from explicit matrix inversion, =emu-gmm= avoids calculating \(V_\mu^{-1}(\theta)\) directly. Instead, it operates on the lower-triangular Cholesky factorization of the design-aware covariance matrix:
\[ V_\mu(\theta) = L_\mu(\theta) L_\mu(\theta)' \]
The system computes a whitened moment vector \(y_\mu(\theta)\) via forward substitution, avoiding the direct inversion of the system:
\[ L_\mu(\theta) y_\mu(\theta) = \mathbb{E}_\mu[\psi(\cdot, \theta)] \]
The objective function is then evaluated strictly as the squared Euclidean \(L_2\) norm of this transformed vector:
\[ Q_\mu(\theta) = y_\mu(\theta)' y_\mu(\theta) = \| y_\mu(\theta) \|^2_2 \]
The GMM estimator is defined as:
\[ \hat{\theta} = \arg\min_{\theta \in \Theta} Q_\mu(\theta) \]
Framing the objective as a pure sum-of-squares allows the system to bypass general-purpose quasi-Newton optimizers and interface directly with high-performance non-linear least-squares solvers (Levenberg-Marquardt, trust-region reflective) [cite:@nocedal-wright06]. These solvers exploit the structure to approximate local updates efficiently using only first derivatives --- derivatives that JAX provides exactly through the residual function \(y_\mu(\theta)\), including the dependence of \(L_\mu\) on \(\theta\) when the weighting strategy calls for it (see below).

** The Weighting Strategy Interface
The choice of weighting policy is encoded as an explicit object that the optimization loop consults at every evaluation. Four strategies cover the standard cases:

- =Identity=: \(\Lambda_\mu(\theta) \equiv I_M\). Useful as a first stage when no preliminary \(V\) is available, or for diagnostic runs.
- =Fixed(V0)=: two-step GMM. The user supplies a precomputed \(V_0\) (typically from a first-stage =Identity= run); \(\Lambda\) is held fixed throughout the second-stage optimization.
- =Iterated(k_max, tol)=: iterated GMM. Alternates between solving \(\hat\theta_k = \arg\min_\theta\, \mathbb{E}_\mu[\psi]'\, V_\mu(\hat\theta_{k-1})^{-1}\, \mathbb{E}_\mu[\psi]\) and refreshing \(V\) at the new \(\hat\theta_k\), up to convergence or \(k_{\max}\) rounds. Each inner solve is a =Fixed= problem; gradients within an iteration are exact.
- =ContinuouslyUpdated= (default): \(\Lambda_\mu(\theta)\) is recomputed at every \(\theta\) evaluation. The residual \(y_\mu(\theta) = L_\mu(\theta)^{-1} \mathbb{E}_\mu[\psi(\cdot,\theta)]\) depends on \(\theta\) through both factors; the NLLS Jacobian computed by JAX captures this dependence exactly, without manual derivation of the \(\nabla_\theta V\) term from Section 4.

A strategy is a JAX pytree carrying its state (precomputed \(V_0\), iteration counter, etc.) so it composes cleanly with =jit= and =vmap=. Section 4's asymptotic-equivalence result guarantees =Iterated= and =ContinuouslyUpdated= share the same limit; the choice between them is a matter of finite-sample behavior and computational cost rather than identification.

** Regularization of \(V_\mu(\theta)\)
When an =EmpiricalCovarianceStrategy= constructs \(V_X(\theta)\) via the pairwise-overlap rule, the result is not guaranteed positive definite in finite samples. The Cholesky pipeline requires \(V_\mu \succ 0\), so the framework applies adaptive regularization. /Read the implication plainly/: under genuine missingness --- the target data, not an edge case --- a binding ridge and therefore the regularisation-adjusted inference of this section are the expected operating regime, and the clean \(\chi^2_{M-K}\) story is the special case. How often \(\tau\) actually binds on realistic designs is an empirical question the validation programme answers directly (the \(\tau\)-binding-frequency column of the Monte Carlo studies, issue #130); until those numbers exist, treat the adjusted p-value as the headline inferential output whenever the mask is non-trivial. The ridge:
\[ V_\mu^\star(\theta) = V_\mu(\theta) + \tau\, \operatorname{diag}(V_\mu(\theta)) \]
with \(\tau\) chosen as the smallest ridge making \(V_\mu^\star\) /both/ positive definite /and/ within a target condition number ceiling (default \(\kappa_\text{target} = 10^6\)). The diagonal-Tikhonov form is scale-equivariant under per-moment rescaling, which matters because moment functions are routinely on heterogeneous scales (a residual in dollars and a residual in shares should not see the same absolute ridge).

The definiteness half of that criterion is load-bearing and easy to get wrong. Feasibility is tested on the /signed/ spectrum (\(\lambda_{\min} > 0\) with a small relative margin, /and/ \(\lambda_{\max} \le \kappa_\text{target}\,\lambda_{\min}\)), *not* on the SVD condition number \(\sigma_{\max}/\sigma_{\min}\). The latter is a ratio of /singular values/ --- absolute eigenvalues --- and is blind to sign: a \(V_X\) carrying one tiny /negative/ eigenvalue can have a perfectly acceptable SVD condition number, so a conditioning-only rule adds essentially no ridge and returns a still-indefinite matrix, whose Cholesky then silently yields =NaN= in the criterion, the \(J\)/\(K\)-statistics, and the bootstraps. Because \(\lambda_{\max} \le \kappa_\text{target}\,\lambda_{\min}\) can hold only when \(\lambda_{\min} > 0\), targeting the signed condition number repairs definiteness and conditioning in one move. This is the contract =_internal/cholesky.py= relies on ("the regularisation layer ensures \(V \succ 0\)"); the per-coordinate-overlap covariances make no PSD guarantee, so the regulariser is the single place that has to enforce it (issue #111).

Alternative regularisers are planned as opt-in; =DiagonalTikhonov= is the only implemented strategy (Section 1's contract table; roadmap in =implementation-plan.org= Section 14):
- *Eigenvalue floor*: clip eigenvalues at \(\epsilon\,\lambda_{\max}(V_\mu)\) via spectral decomposition. Transparent; \(O(M^3)\).
- *Higham nearest-PSD* [cite:@higham88]: project to the nearest positive semidefinite matrix in Frobenius norm. Most principled, slowest.

To avoid introducing non-smoothness into the objective surface, \(\tau\) is selected once at an anchor point and held fixed thereafter. /Implemented policy/: the anchor is the \(V\) built by the covariance strategy at \(\theta_{\mathrm{init}}\), for *every* weighting strategy (=Fixed= / =Iterated= included --- the earlier sketch of a first-stage-\(\hat\theta_1\) anchor for those strategies was simplified away; see =api-sketch.org= Section 6 question 3). =build_estimator= computes the anchor once at /factory/ time from its template measure, so repeated calls of the returned estimator share one \(\tau\) --- deliberate, for cross-fit comparability (see issue #123 for the semantics note). The chosen \(\tau\), the achieved \(\kappa(V_\mu^\star)\), and the Cholesky pivot floor are surfaced on =Diagnostics= at convergence; a binding ridge is a diagnostic flag rather than silently absorbed.

*Implementation note --- diagonal-V dispatch.* The canonical form \(V + \tau\,\operatorname{diag}(V)\) is degenerate when \(V\) is itself diagonal: it collapses to \((1+\tau)\,V\), which leaves \(\kappa(V^\star) = \kappa(V)\) regardless of \(\tau\), so the bisection over \(\tau\) silently saturates at \(\tau_{\max}\) without ever achieving the target. =DiagonalTikhonov= therefore dispatches: it detects near-diagonal \(V\) with relative tolerance \(10^{-12}\) on the off-diagonal entries and switches to the additive eigenvalue shift
\[ V^\star = V + \tau\,\frac{\operatorname{tr}(V)}{M}\,I, \]
which actually moves the spectrum. The two branches agree on the isotropic limit \(V = c\,I\), so the dispatch introduces no discontinuity there. The trade-off is real: the canonical (non-diagonal) branch is per-moment scale-equivariant (rescaling moment \(j\) by \(s_j\) rescales \(V^\star_{jj}\) by \(s_j^2\) identically to \(V_{jj}\)); the diagonal branch is only uniformly scale-equivariant (rescaling all moments by the same \(s\) preserves the ridge shape, but heterogeneous per-moment rescaling does not). The diagonal branch is the price of a working bisection on structurally diagonal \(V\); in practice the dispatch triggers in =AnalyticalCovariance= setups where the user supplies a diagonal \(V\) directly, not in the empirical hot path. See =src/emu_gmm/regularization.py= for the implementation.

Regularization alters the limit law of \(Q_\mu(\hat\theta)\): the \(\chi^2_{M-K}\) distribution becomes a weighted-\(\chi^2\) whose weights depend on \(\tau\). For non-binding \(\tau\) the deviation is negligible; the inference engine reports both the nominal \(\chi^2\) critical value and a regularization-adjusted critical value when \(\tau\) exceeds a configurable threshold (default \(\tau > 0.01\)).

** Identification Diagnostics
When a =StructuralModel= is integrated against an =AnalyticalContext= parameterised at a known true value \(\theta_0\), the population moment condition \(\mathbb{E}_{\mu_\text{analytical}}[\psi(\cdot,\theta_0)] = 0\) holds exactly. The framework leverages this to provide:

1. *Local boundary pathology* --- gradient zero, objective positive. \(\nabla_\theta Q(\theta^\star) = 0\) but \(Q(\theta^\star) > 0\) at the reported solution \(\theta^\star \neq \theta_0\). Interpretation: local identification failure or multi-modal global non-identification; the moment system has a stationary point that is not the truth.
2. *Conditioning pathology* --- Cholesky pivot failure. The factorization \(V_\mu(\theta) = L_\mu(\theta) L_\mu(\theta)'\) encounters a zero or sub-tolerance pivot. Interpretation: moment redundancy or collinearity; the framework reports the column index at which the pivot failed, mapping it back to the offending moment in the =StructuralModel=. Distinct from the pairwise-overlap regularization case above: a pivot failure on an =AnalyticalContext= is a structural identification problem, not a sampling artifact.
3. *Numerical instability* --- gradient nonzero at the known root. \(\nabla_\theta Q(\theta_0) \neq 0\) when evaluated at the supplied truth. Interpretation: quadrature insufficiency, severe parameter scale disparities, or floating-point underflow --- solver mechanics rather than theory.

Each diagnostic surfaces a structured report (failure category, offending dimension index, residual magnitude) rather than a generic optimization failure. Combined with the analytical-context round-trip, this transforms identification analysis from an /a priori/ derivation into an /a posteriori/ test that runs against the actual implementation.

** The Overidentification Statistic
Under the null hypothesis that the parametric model is correctly specified, and with \(V_\mu(\theta)\) used unregularized, the minimized sample criterion function converges cleanly to a chi-squared distribution:
\[ Q_X(\hat{\theta}) \xrightarrow{d} \chi^2_{M - K}. \]
When the variance has been regularized to \(V^\star = V + \tau\,R(V)\), the limit is a generalised chi-squared: \(Q_X(\hat\theta) \xrightarrow{d} \sum_i w_i Z_i^2\) where the \(w_i\) are the (at most \(M - K\)) non-zero eigenvalues of \(P\,\tilde V\,P\) in the whitened coordinates \(\tilde y = L_\star^{-1} m\), with \(P\) the orthogonal projector onto the orthogonal complement of \(\operatorname{col}(L_\star^{-1} G)\). At \(\tau = 0\) all surviving weights collapse to 1, recovering \(\chi^2_{M-K}\); the derivation is =mcar-asymptotics.org= Theorem 6.

The framework reports the generalised-chi-squared survival as =EstimationResult.J_pvalue_adjusted=, computed by the Welch--Satterthwaite moment-matching approximation [cite:@satterthwaite1946]: match the first two moments of \(\sum_i w_i Z_i^2\) (mean \(\sum w_i\), variance \(2 \sum w_i^2\)) to a scaled chi-squared \(c \cdot \chi^2_v\) with \(c = \sum w_i^2 / \sum w_i\) and \(v = (\sum w_i)^2 / \sum w_i^2\). The implementation is =regularization_adjusted_pvalue= in =src/emu_gmm/diagnostics.py= and is jit/vmap-traceable end-to-end. Davies' / Imhof's exact CDF was considered and closed as not needed (owner decision 2026-06-11, on the #130 validation evidence: Welch--Satterthwaite calibrates exactly where the fixed-tau limit applies --- uniformity KS 0.211 nominal vs 0.085 adjusted over the full law in the engineered binding regime; see the validation report, study 5). Both =J_pvalue= (the nominal \(\chi^2_{M-K}\) value) and =J_pvalue_adjusted= are surfaced on every result so the user can compare them and judge whether the regularisation is materially distorting inference.

** Architectural Core Highlights

1. *No External Scalar Multipliers*: Because \(V_\mu(\theta)\) represents the variance of the sample moment vector itself (scaling at \(O(1/N)\)) rather than the variance of an individual observation, the standard textbook \(N\) multiplier is absorbed into the variance definition; \(Q_X(\hat{\theta})\) converges to a \(\chi^2\) natively. This is a convention choice rather than a free lunch --- it differs from the [cite/t:@hansen82] / [cite/t:@hayashi00] textbook normalisation, and the divergence should be remembered when porting code from elsewhere.
2. *Asymmetric Scale Resolution*: The interaction of the coordinate scaling inside \(G_\mu(\theta)\) and the scaling inside \(V_\mu(\theta)\) ensures that the sample information matrix properly weighs the relative data density across different variables without requiring listwise deletion.

** NaN is an event, not a value (#140)

A NaN in a diagnostic output must either propagate loudly or be counted explicitly --- it must never fold into a rate or a mean. A diagnostic NaN encodes a /named event/ (the quantity is undefined, or a policy fired), not a numeric value; the hazard motivating the rule is that numpy comparisons silently coerce NaN to =False= /inside a live denominator/, so an unguarded aggregation fabricates a rate (a just-identified model's all-NaN J p-values would read as a 0% rejection rate).

- Producer obligation :: every NaN-emitting site documents which event its NaN encodes, paired with a named flag where feasible. Current emitters: NaN standard errors under the diagnose-loudly indefinite-meat policy (#138; the flag is =sigma_meat_indefinite=, carried onto =FitRecord= per #143), and NaN p-values on just-identified problems (\(\mathrm{df} = 0\): the J p-values in =estimator.py=, =k_statistic='s =p_S=).
- Consumer obligation :: every aggregation over potentially-NaN diagnostics carries an explicit valid count, never a silent denominator (the =n_valid_*= accounting in the =emu_gmm.studies= summarizers); and a set/decision consumer never lets a NaN choose a side --- =k_confidence_set= excludes NaN-p grid points from the set /and/ surfaces them (=n_invalid=, =invalid_indices=, a summary warning), so an invalid point is neither accepted nor rejected.

The convention's first full-stack exercise is the #130 validation harness, whose mask-only zero-row events and validity cells apply exactly this named-event accounting end to end.

** Open Theoretical Questions
The pairwise-overlap covariance machinery introduced in Section 2 sidesteps listwise deletion at the cost of theoretical loose ends that deserve treatment before the framework is leaned on for primary inference:

1. *Asymptotic theory under per-coordinate missingness.* The classical GMM consistency and asymptotic-normality results assume the moment vector \(\bar m_X(\theta)\) is the sample mean of an iid (or appropriately mixing) sequence. Under per-coordinate missingness with selection indicators \(d_{ij}\), the relevant sequence depends on the missingness mechanism. MCAR (selection independent of \(\theta\) and \(\psi\)) carries the standard results through with \(V\) rescaled by per-coordinate effective sample sizes; MAR and MNAR require additional structure. The framework currently makes no claim about consistency outside MCAR.

2. *Asymptotic target of \(V_X(\theta)\) under the pairwise-overlap rule.* The per-coordinate normalization \(1/(N_j N_k)\) produces a consistent estimator of \(\operatorname{Var}[\bar m_X(\theta)]\) under MCAR with bounded inclusion probabilities. The general case requires showing that overlap fractions \(N_{jk}/N\) converge to positive limits --- a condition we can state but have not verified across all design types on the v1/v2 ladder.

3. *Finite-sample versus asymptotic positive-definiteness.* The diagonal-Tikhonov regularization handles finite-sample PD failure but distorts the J-statistic's limit law. The asymptotic PD of \(V_X(\theta_0)\) under standard conditions (MCAR, bounded weights, finite fourth moments) is straightforward; the rate at which finite-sample PD becomes reliable in practice is not characterized here.

Items (1) and (2) under MCAR --- plus the J-statistic limit and asymptotic positive-definiteness of \(V^\text{lim}\) --- are established in the companion note =docs/mcar-asymptotics.org=. The remaining quantitative and MAR/MNAR pieces are deferred to later revisions of that note. The implementation paths described in this document are unchanged by any of this: the open items affect what we /claim/ the estimator is doing, not what the code does.

** Prospective sketch: =EstimatorLaw= --- laws of statistics as a first-class interface

/Status: PRELIMINARY AND PROSPECTIVE. Recorded 2026-06-09 as the "ParameterLaw" idea; substantially revised after the design discussion of 2026-06-10 (renamed, codomain generalised, the adoption gate formally tripped --- dispositions below). Still not scheduled or an API commitment; the design pass is tracked as a v2.1 issue./

Every estimator in this framework constructs a random variable: \(\hat\theta = T(X_1, \dots, X_N)\) is a measurable map, and its law is the pushforward of the sampling distribution of the data through \(T\). The framework's identity is that \(\mathbb{E}_\mu\) over the /data/ space is one interface with three implementations; the observation motivating this sketch is that the /output/ side has no counterpart. The package holds several partial representations of the law of \(\hat\theta\), none of them first-class:

1. /The asymptotic law/: \(\mathcal{N}(\theta_0, \Sigma_\theta)\) to first order --- =Sigma_theta= plus the delta method (=functional_se= is exactly "integrate \(f\) against the Gaussian law, to first order").
2. /Bootstrap laws/: =cluster_bootstrap='s =theta_boot= is an empirical measure on \(\Theta\) approximating the same law conditionally on the sample (the =Bootstrap*= functionals in =inference/adaptive.py= are already a query algebra over it); the wild bootstrap carries the law of \(Q(\hat\theta)\).
3. /The repeated-sampling law/: the =emu_gmm.studies= records are the literal finite-sample law under a known DGP; the SE/MC-SD calibration ratio is precisely a comparison of law (1)'s scale against law (3)'s.

*The sketch.* An =EstimatorLaw= interface mirroring the =Measure= trick --- one query algebra, swappable measures, this time over the statistic's codomain \(S\): =law.cov()=, =law.quantile(f, q)=, =law.prob(...)=, =law.sample(key, n)=, =law.given(event)=; implementations =AsymptoticLaw= (Gaussian; delta-method queries), =BootstrapLaw= (empirical on replicate draws), =MonteCarloLaw= (empirical on repeated-sampling records). The implementation determines the epistemic grade of the answer.

*Design dispositions (2026-06-10 discussion; supersede the original complications list):*

- Name :: =EstimatorLaw=, not "ParameterLaw". It is the frequentist law of the /estimator/ under a DGP, not a belief over \(\theta\); the original name was itself the Bayesian misreading the idea must fight.
- Conditioning :: The law conditions on the estimator's /configuration/ (weighting, regularisation, anchor policy, ...) --- analyst-chosen knobs are simply part of \(T\)'s identity, and the carrier records that identity in full (the =_emu_gmm_factory_spec= attribute is the existing seed of this). Distinguish /realized/ data-dependent conditioning variables that an execution model froze (the factory-shared ridge anchor of #142, where the sampled object silently became the conditional law of \(\hat\theta\) given replicate 0's \(\tau\)): those must be recorded explicitly, with conditional-vs-marginal surfaced rather than implied. #142 is the field evidence that the two laws differ measurably.
- The codomain is generic :: \(S\) need not be \(\Theta\). A confidence set is itself a statistic --- a random /closed set/, with a well-defined law (capacity functionals; Molchanov; in econometrics Beresteanu--Molinari): coverage is the capacity functional at \(\{\theta_0\}\), and an MC validation of =k_confidence_set= is a query battery against that law. Moment-inequality estimands (set identification) live in the same frame. Tuples are statistics too (\(S = \Theta^2\)), which is where CRN-paired contrasts live. Point, set, and tuple cases are one abstraction with different \(S\).
- Diagnostic events ride as a product :: the #140 convention means a replication yields \(\hat\theta\) /and/ named-event flags (binding ridge, indefinite meat, unsupported rows), so the honest object is the /joint/ law on \(\Theta \times \{0,1\}^E\) --- never a law that folds events into rates. Conditional queries (=law.given(binding)=) are then native; they are also the queries the #130 harness actually needed and hand-rolled.
- Closure and grade-dependence :: =given= returns another =EstimatorLaw= (the algebra composes). Availability is grade-dependent: most conditionals do not exist in closed form at the asymptotic grade --- the implementation /refuses/ rather than approximates, which is the epistemic-grade semantics working as intended.
- Couplings must verify their probability space :: the joint law of \((T_A(X), T_B(X))\) is NOT determined by the marginals; it exists in the records only because the =replicate= CRN contract aligns draws across arms. Laws carry their key/provenance, and the joint-law constructor verifies same-space before zipping --- pairing un-coupled stacks and computing "flips" across unrelated draws is a silent-validity error of exactly the class this package hunts (cf. commitment 9's vacuous-fixture lesson). At the asymptotic grade the coupled object has its own closed form (stacked influence functions), so the algebra stays uniform across grades.
- Weak identification guards :: under weak identification the point-statistic's law exists and answers queries that do not mean what a consumer thinks (the real-data Euler test's boundary-collapsed \(\hat\sigma\) vs its honest robust set \([<0.05, 9.5]\)). Not a rival philosophy --- the set-statistic's law is the same abstraction --- but the point-law implementations must consult the weak-ID diagnostics and diagnose loudly (#138's policy, #41's dispatch precedent) rather than answer blithely.

*Adoption gate: TRIPPED 2026-06-10* (the Arc-2 work delivered the predicted duplication, over-fulfilled: five carrier types --- =Sigma_theta=+delta-method, =theta_boot=+=Bootstrap*= functionals, =MCRecords=+summarizers, the wild-bootstrap law of \(Q\), and the test-inversion dual --- plus three more hand-rolled empirical-law queries in the #130 validation harness). Disposition per the spare-interfaces philosophy: a /narrow/ v2.1 design pass --- signature-generic in \(S\), point-implemented: one empirical-law carrier (stacked draws \(\times\) event flags \(\times\) validity counts \(\times\) provenance), =summaries.py= and the =Bootstrap*= functionals retrofitted onto it, =given= and verified couplings included because those are the proven consumers. The =AsymptoticLaw= wrapper (its queries already exist as =functional_se=) and the random-set implementation wait for the consumers that would pull them.

* Boundary Interfaces, Named Tensors, and PyTree Topology

To maintain mathematical legibility without introducing non-compilable runtime bottlenecks, =emu-gmm= eschews traditional dataframe paradigms (like =pandas=) inside the computation zone. Instead, the framework utilizes JAX-native named tensor architectures (=haliax= [cite:@hall23]) and type-level axis verification (=jaxtyping= [cite:@kidger22]).

This ensures that labels, dimension names, and missingness masks are fully integrated into JAX's PyTree layout, allowing structured datasets to pass seamlessly through =jit=, =vmap=, and automatic differentiation boundaries. Pandas-labelled tensors remain available at the I/O boundary (load, save, summarize) via the workspace's =jax-pandas-bridge= utilities; they do not enter the hot path.

** Labelled-Array Boundary as a Design Goal

/Added 2026-06-09 (owner directive)./ First-class handling of labelled arrays on *both* sides of the boundary is a design goal, not a convenience: a user working in =pandas=, =polars=, or the workspace's =DataMat= / =DataVec= containers should be able to hand their objects to =estimate(...)= and get labelled objects back, with the labels they supplied --- never positional fallbacks when real labels were available. The commitments:

- Ingress :: A single adapter registry (today =_internal/labels.py='s =normalise_*= family) dispatches on input type. Supported carriers: plain JAX arrays, =haliax.NamedArray=, =pandas.DataFrame= / =Series= (column names \(\to\) Variables labels, index \(\to\) Obs), =polars.DataFrame=, and =DataMat= / =DataVec=. Third-party carriers are recognised via optional imports --- never new runtime dependencies; an absent backend raises a helpful error only when actually handed such an object.
- Label fidelity :: Labels supplied at ingress survive to egress. The /Variables/ labels from a DataFrame must thread through =LabelContext= into =EstimationResult= rather than being regenerated positionally (the v1 implementation drops them; tracked as the labelled-boundary issue).
- Egress :: =haliax.NamedArray= remains the internal carrier; materialisation is backend-parametrised (=result.to_pandas()= today; =to_polars()= / =to_datamat()= or a =to_frame(backend=...)= dispatcher as the registry grows). =coef_table='s direct-pandas exception stands (see below), generalised per backend.
- Hot path purity :: Adapters are strictly boundary translations. Nothing in =jit= / =vmap= / AD ever sees a dataframe object --- this subsection changes the *coverage* of the boundary, not its location.

** Axis Specifications and Haliax Integration
Dimensions are promoted from implicit integer indices to first-class compile-time constants. We define the foundational axis taxonomy for any estimation instance:

#+BEGIN_SRC python
import haliax as ha

Params       = ha.Axis("parameters",      size=K)
ParamsDual   = ha.Axis("parameters_dual", size=K)
Moments      = ha.Axis("moments",         size=M)
MomentsDual  = ha.Axis("moments_dual",    size=M)
Obs          = ha.Axis("observations",    size=N)
#+END_SRC

The =Dual= axes carry the /same size/ as their primary counterparts and are used as the second index of symmetric matrices (\(\Lambda\), \(\mathcal{I}\), parameter covariances). Naming both indices of a symmetric matrix with the same string causes =ha.dot= to alias the contraction and silently produce wrong results --- the dual-name convention prevents that pitfall.

The structural components yield outputs mapped across these formal dimensions. The Sample Jacobian is typed natively as a two-dimensional named array:
\[ G_X : \Theta \to \text{ha.NamedArray}[\text{Moments}, \text{Params}] \]

The weighting matrix is typed as:
\[ \Lambda_X : \Theta \to \text{ha.NamedArray}[\text{Moments}, \text{MomentsDual}] \]

Computing the sample information metric requires renaming the duplicated axes of \(G\) before contraction:
\[ \tilde G_X = G_X.\text{rename}(\{\text{Moments}: \text{MomentsDual},\; \text{Params}: \text{ParamsDual}\}) \]
\[ \mathcal{I}_X(\hat\theta) = \text{ha.dot}(\text{Moments},\, G_X,\, \text{ha.dot}(\text{MomentsDual},\, \Lambda_X,\, \tilde G_X)) \]
producing a =NamedArray[Params, ParamsDual]= naturally, without further renaming.

Changing \(K\), \(M\), or \(N\) between runs triggers JIT recompilation; JAX's shape-signature cache amortises the cost across repeated estimations at the same dimensions, so the recompile bites only at the start of a parameter / moment sweep.

** Labeled Missingness Masks as Tensor Fields
Rather than relying on floating-point =NaN= sentinels that contaminate the automatic differentiation tape, the missingness layout is captured as a dedicated boolean structure. At the I/O boundary the spec is
\[ \text{mask} \in \text{ha.NamedArray}[\text{Obs}, \text{Moments}, \text{bool}], \]
i.e. =EmpiricalMeasure.from_pandas= / =from_nan_aware= / =from_numpy= constructors accept named-array boolean masks (and derive one from =~isna()= when handed a NaN-aware =DataFrame=). /Internal storage/ inside the =EmpiricalMeasure= dataclass is the plain =Float[Array, "N M"]= 0/1 form (=mask: Float[Array, "N M"]= in =src/emu_gmm/measures/empirical.py=); this is the form that goes through =jit= and =vmap=, multiplied directly into per-observation \(\psi\) values inside =expectation=, =jacobian=, =IIDCovariance=, and =ClusteredCovariance=. The conversion is a one-shot boundary translation; the named-array carrier survives only in metadata.

The =EmpiricalCovarianceStrategy= leverages this mask to execute coordinate-wise element selection entirely within compiled tensor primitives, enabling JAX to perform highly parallelized data filtering across SIMD lanes or hardware accelerators without dropping out of the execution graph.

NaN remains the natural sentinel at the /I/O boundary/: many real-data workflows (asset returns for non-holders, seasonal residuals for households outside a contemporaneous holding) arrive as NaN-typed cells in a =pandas.DataFrame=. The =EmpiricalMeasure.from_pandas= and =from_nan_aware= constructors translate between the two conventions in a single pass: the mask is inferred from =~isna()=, and the NaN cells in the stored =x= are replaced with the per-column mean of that column's /observed/ rows --- the =column_mean_sentinel= used by =safe_x_for_psi= in =src/emu_gmm/_internal/nan_safety.py= --- so the hot path carries the boolean / 0--1 mask above over an =x= that is already NaN-free.

The sentinel is deliberately *not* zero. This is the first half of a /double where/. A partial residual such as =log=, =1/x=, or =sqrt= is singular at zero, so a zero-filled masked cell would feed =-inf= / =inf= into =psi=; and although the output mask zeroes the /primal/ contribution, reverse-mode AD would still propagate a =0 * inf == NaN= cotangent through the untaken =where= branch. The observed-column mean lies inside the domain of those residuals whenever any observed cell does, which keeps the gradient well-defined --- substituting a fixed =0.0= here was the prior behaviour and silently produced NaN cotangents (see the comment in =from_pandas=). The substitution is applied only when the mask is /inferred/ from NaN (=nan_aware= with no explicit mask); supplying an explicit mask alongside NaN-laden =x= raises rather than silently rewriting an unobserved value into an observable cell.

The second half of the double where guards the contraction itself: inside =expectation=, =jacobian=, =IIDCovariance=, and =ClusteredCovariance=, =jnp.where(mask > 0, psi, 0.0)= zeroes residual cells prior to multiplication, since =0 * NaN == NaN= means a =psi= that still returns NaN at a masked-out row would otherwise poison both the moment sum and reverse-mode autodiff. Note that this output guard alone is /not/ sufficient: it protects only against a measure whose stored =x= is NaN-free at masked cells (as the standard constructors guarantee). A measure built by calling =EmpiricalMeasure(x=..., mask=...)= directly, with singular values (e.g. exact zeros) standing in for missing /data/ and no NaN to trigger the sentinel substitution, can still poison reverse-mode AD through a singular =psi=. The robust contract is therefore that missing /data/ be presented as NaN, so =safe_x_for_psi= can substitute an in-domain sentinel before =psi= sees it. Note that a covariance strategy cannot in general recover this from the moment-level =mask=: =mask= is =(N, M)= (per /moment/) while the =psi= input =x= is =(N, D)= (per /data column/), and =M = D= only in the degenerate identity-residual case --- so "sanitise where =mask == 0=" is not a general substitute for NaN-marking the underlying data. The standard constructors discharge this contract automatically; a hand-built measure must NaN-mark its missing cells.

** Structured Egress and Metadata Binding
Because =haliax.NamedArray= instances and custom dataclasses decorated with =@jdc.pytree_dataclass= are fully compliant JAX PyTrees, the final outputs of the estimation loop require zero reconstruction or mapping steps. The =InferenceEngine= returns parameter arrays, covariance matrices, and overidentification statistics that inherently carry their coordinate strings, producing self-documenting diagnostic footprints.

One deliberate exception: =EstimationResult.coef_table= returns a =pandas.DataFrame= indexed by parameter name directly (columns =estimate=, =std_error=, =t_stat=, =p_value=), with the index hand-rolled from =LabelContext.param_names= rather than routed through a =haliax.NamedArray= carrier. The egress payload is a heterogeneous (=float64=, p-values from =scipy.stats=) table consumed at the human / pandas boundary, so the NamedArray detour earns nothing; the implementation in =src/emu_gmm/types.py= treats =coef_table= as a cached property that materialises the DataFrame directly. The general "NamedArray inside, pandas at the I/O boundary" framing still applies to =Sigma_theta=, =V_X=, and per-moment diagnostics --- only the coefficient summary skips the carrier.
