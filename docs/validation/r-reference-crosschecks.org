#+TITLE: Cross-validating emu-gmm against R reference implementations
#+AUTHOR: emu-gmm
#+OPTIONS: toc:2

/Status: IN PROGRESS. Tier 1 (linear over-identified IV vs R =gmm= / =AER=) and
Tier 2 (multi-asset Euler / Hansen–Singleton vs R =gmm=) have landed
(=tests/validation/test_r_reference_{linear_iv,euler}.py=). Tier 3 (=sandwich= SE
conventions) is scoped below./

* Why

Independent confirmation that emu-gmm's numbers are correct, and a controlled
way to find where our *conventions* differ from the reference econometrics
literature. R is the natural oracle: Pierre Chaussé's =gmm= package (JSS 2010)
is the canonical GMM implementation, =AER::ivreg= is the reference 2SLS, and
=sandwich= is the reference for robust/clustered standard errors.

The division of labour deliberately mirrors =tests/test_estimator_realdata.py=:
the R fit is run *once* to freeze reference numbers + provenance into a JSON
sidecar, and the committed acceptance test consumes those frozen numbers, so
**CI needs no R toolchain**. Only regeneration needs R.

* Running R in this environment

R is *not* available through an environment module (=module add R=) here, and
CRAN is blocked by the agent egress policy (=cran.r-project.org= → HTTP 000).
The reachable route is the Ubuntu ~r-cran-*~ debs, installed via apt through the
proxy:

#+begin_src sh
apt-get update
apt-get install -y r-base-core r-cran-gmm r-cran-aer r-cran-sandwich r-cran-lmtest
#+end_src

This gives R 4.3.3 with =gmm= 1.8, =AER= 1.2.12, =sandwich= 3.1.0, =lmtest=
0.9.40 — enough for Tiers 1–3. (=r-cran-momentfit= is not in the reachable
repos; =gmm= covers the same ground.)

* Tier 1 — linear over-identified IV (LANDED)

** Setup

A textbook over-identified IV DGP (=scripts/gmm_reference/gen_data.py=, seeded,
committed as =tests/data/gmm_linear_iv.csv=): one endogenous regressor =xe=,
three excluded instruments =z1,z2,z3= (plus the constant), truth =(b0,b1) =
(1,2)=. The moment vector is =g_i = (1,z1,z2,z3)_i * (y_i - b0 - b1*xe_i)=, so
=M=4=, =K=2=, and there are two over-identifying restrictions (=J ~ chi^2_2=).
The identical CSV is read by both R (=scripts/gmm_reference/reference.R=) and
emu-gmm (the test), with a =sha256= provenance guard so the two cannot silently
drift.

** Results (n=500, seed 20260704)

| quantity      |   emu-gmm |    R gmm | note                         |
|---------------+-----------+----------+------------------------------|
| 2SLS b1 (matched weight) | 2.0241896 | 2.0241896 | =AER::ivreg=; exact to 1e-6  |
| CUE b1        |    2.0204 |   2.0183 | gmm =type="cue"=             |
| iterated b1   |    2.0269 |   2.0242 | gmm =type="iterative"=       |
| iterated se(b1) |   0.0479 |   0.0492 | ~2.6% (convention)           |
| iterated J    |     3.613 |    3.731 | both → chi^2_2, p≈0.155–0.164 |

** Findings

1. *The GMM solver is correct.* With the 2SLS weight =(Z'Z/n)^{-1}= held
   identical on both sides, emu-gmm reproduces =AER::ivreg= to machine precision
   (7+ digits). This isolates and confirms the estimator math, independent of
   any covariance-estimator convention.

2. *Commitment 9 (the =N= scaling) is validated against an independent
   implementation.* emu-gmm folds the per-coordinate =N_j= into =V_X= and
   carries no explicit =N=, so =objective_value = mbar' V_X^{-1} mbar=. R's =gmm=
   reports =J = n * gbar' S^{-1} gbar=. These land on the *same scale* (≈3.6 vs
   ≈3.7, both referenced to =chi^2_2=, p≈0.16) — a factor-of-=n= (=500=) error in
   our convention would put =J= near 0.007 or near 1900. It does neither. This is
   exactly the class of bug the downstream-contract section of =CLAUDE.md= warns
   about, now checked externally.

3. *The residual ~0.1–3% gap is a convention, not an error.* It is small
   (~0.1% on the CUE point, ~2–3% on SE / J) and statistically negligible
   (~0.04 SE), but systematic. Its source is *not* fully pinned down, and one
   candidate has been *ruled out*: moment-covariance centering. emu-gmm's
   =IIDCovariance= is uncentered by default and now offers a =centered=True=
   knob (below) matching R =gmm='s =centeredVcov=TRUE= convention; but directly
   toggling it shows centering changes =V_X= by only ~0.4% here, moves =J= by at
   most ~a quarter of the gap, and leaves the emu CUE *point estimate*
   byte-identical (still =b1 ≈ 2.0204= vs gmm =2.0183=). Symmetrically, gmm's own
   =centeredVcov= toggle did not move its numbers on this fit. So the residual
   emu-vs-gmm CUE difference stems from something else --- most plausibly a
   difference in how the two packages construct / optimise the CUE weight
   =V(theta)= (or a small-sample weight factor) --- and remains under
   investigation. What is *decisive* is claim 1: under a matched weight the two
   agree to machine precision, so the GMM solver is correct; the convention
   differences live entirely in the weight construction.

** The =centered= knob on =IIDCovariance=

Motivated by this cross-check, =IIDCovariance(centered=True)= subtracts the
per-coordinate mean moment =m_j = (1/N_j) sum_i d_ij w_i psi_j= before forming
the pairwise-overlap outer products, giving the numerator
=sum_i d_ij d_ik w_i^2 (psi_j - m_j)(psi_k - m_k)= --- the centered
moment-covariance convention (R =gmm='s =centeredVcov=TRUE=, most textbooks).
Both forms are PSD by construction (a sum of masked outer products) and agree
asymptotically at =theta_0= (where =E[psi]=0=); they differ by =O(mbar^2)= at an
over-identified =theta_hat=. Default stays =False= (uncentered, marginally
better finite-sample conditioning) --- no behaviour change. Note the honest
caveat above: the knob supplies the centered *convention* (useful to match a
reference's vcov / SE reporting), but on this well-fitting example it does *not*
by itself reconcile the CUE point estimate with =gmm=.

** Regenerate

#+begin_src sh
python scripts/gmm_reference/gen_data.py         # -> tests/data/gmm_linear_iv.csv
Rscript scripts/gmm_reference/reference.R        # -> tests/data/gmm_linear_iv_reference.json
#+end_src

The test asserts the CSV =sha256= matches the JSON's recorded value, so a data
change that isn't accompanied by an R re-run fails loudly.

* Where assumptions may matter (the divergence catalogue)

These are the high-information axes — several are places emu-gmm is deliberately
*more general* than R GMM, so a mismatch is expected and worth documenting, not a
bug.

| axis | R (gmm/sandwich) | emu-gmm | verdict |
|------+------------------+---------+---------|
| Moment-covariance estimator | centered-family =S= | uncentered PSD =sum w_i^2 g g'/(N_j N_k)= | ~2–3% SE/J gap on over-ID fits; the Tier-1 finding |
| Per-moment missingness | listwise / complete-case | per-coordinate =mask=, unequal =N_j= (commitment 9) | no R analog; different estimand under MCAR (=mcar-asymptotics.org=). Balanced data must agree. |
| Weight regularisation | none | =DiagonalTikhonov= anchor-freeze (commitment 3) | agree when well-conditioned (τ≈0); diverge by design when ill-conditioned |
| Clustered dof | scalar =G/(G-1)= | per-*pair* effective counts =H_{c,jk}= (commitment 10) | match under balanced observability; emu generalises |
| SE basis | analytic sandwich | =G'ΛG=, never numerical Hessian (commitment 5) | should match; an equality check |
| HAC / Newey–West | =sandwich::NeweyWest= | *not implemented* | out of scope — a gap, not a cross-check |
| σ>0 parameter | =log=-reparameterise in gmm | =Positive= manifold + =RiemannianLM= | reparam-vs-manifold is a targeted check |

* Tier 2 — multi-asset Euler / Hansen–Singleton (LANDED)

** Setup

A Hansen–Singleton-style consumption-Euler / SDF estimation using the *exact*
DGP and moment of the bundled =src/emu_gmm/examples/euler.py= (the shipped
example three acceptance tests already drive), so the cross-check validates the
*shipped model*, not a re-implementation. Data
(=scripts/gmm_reference/gen_euler_data.py= → =tests/data/gmm_euler.csv=,
n=2000): columns =c_t, c_next, r0, r1, r2=; per-observation moment
=psi_j = beta*(c_next/c_t)^(-gamma)*(1+r_j) - 1= for the three assets, so =M=3=,
=K=2= (=beta=, =gamma=), one over-identifying restriction (=J ~ chi^2_1=). Truth
=(beta, gamma) = (0.96, 2.0)=. The emu test imports =euler_residual= verbatim;
the R reference (=reference_euler.R=) writes the same moment as a =gmm= function.

** Results (n=2000, seed 20260704, start (0.95, 1.5))

| quantity     | emu-gmm | R gmm |
|--------------+---------+-------|
| CUE beta     | 0.954694 | 0.954694 |
| CUE gamma    | 1.763325 | 1.763336 |
| CUE se(beta) |  0.00852 |  0.00852 |
| CUE se(gamma)|  0.44600 |  0.44600 |
| CUE J        |   1.0904 |   1.0910 |
| CUE p        |   0.2964 |   0.2963 |

** Findings

emu-gmm and =gmm= agree to *~5–6 significant figures* on this nonlinear fit —
tighter than the linear-IV case. The reason is instructive: at the
over-identified optimum the moment mean =mbar= is ~0, so the centered
(=sum (g-gbar)(g-gbar)'=) and uncentered (=sum g g'=) covariance estimators
nearly coincide, collapsing the Tier-1 convention gap. Commitment 9 holds on the
nonlinear criterion too: =J ≈ 1.09=, =dof = 1=, both → =chi^2_1=, =p ≈ 0.296= —
no factor-of-=n= (=2000=) discrepancy. This is the strongest single confirmation
of the framework's nonlinear pipeline: the *shipped* Euler example reproduces an
independent, peer-reviewed GMM implementation to machine-adjacent precision.

** Regenerate

#+begin_src sh
python scripts/gmm_reference/gen_euler_data.py    # -> tests/data/gmm_euler.csv
Rscript scripts/gmm_reference/reference_euler.R   # -> tests/data/gmm_euler_reference.json
#+end_src

* Tier 3 — sandwich SE conventions (SCOPED)

For one fixed fitted model, compare emu-gmm's =IIDCovariance= /
=ClusteredCovariance= SEs against =sandwich::vcovHC= (HC0–HC3) and =vcovCL= (with
its =cadjust= / =G/(G-1)= knobs) to pin down *which named convention* emu-gmm
implements. (The Tier-1 centered-vs-uncentered question is partly resolved: the
=centered=True= knob has been added to =IIDCovariance= --- but the cross-check
also showed centering is *not* the source of the emu-vs-gmm CUE gap, so the open
follow-up is to identify what in the CUE weight construction *is*.)
